interface:
  display_name: "YUP Plugin Builder"
  short_description: "Build, stage, install, and verify YUP plugin artifacts"
  default_prompt: "Use $yup-build-plugin to build, test, install, and verify this EHL YUP plugin."
